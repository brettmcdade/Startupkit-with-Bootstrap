'use strict';

// node modules (shim)
var $ = require('jquery');


// common components
var example          = require('./modules/example');

// init
example.init();


window.location;

// force events
$(window).scroll();
$(window).resize();